{
  "tasks": {
    "build": "pnpm install && pnpm build",
    "download_diffs_codespace": {
      "label": "Download Diffs from Codespace",
      "type": "shell",
      "command": "git fetch origin && git diff origin/main"
    },
    "push_diff_codespace": {
      "label": "Push Diffs to Codespace",
      "type": "shell",
      "command": "git add . && git commit -m 'Sync diffs' && git push origin main"
    }
  }
}
